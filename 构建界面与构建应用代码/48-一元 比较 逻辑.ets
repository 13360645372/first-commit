// 一元运算符（++ --）
// 符号在前后的区别
// n++ 先赋值再自增
// ++n 先自增再赋值


// 比较运算符（> >= < <= == !=） 返回一个布尔值（true / false）
// == != 判断是否相等（判断数据，也会用来判断字符串


// 逻辑运算符 （&& || !）
// && 与，都真才真
// || 或，一真才真
// ! 非， 全非

@Entry
@Component
struct Index {

  build() {

  }
}